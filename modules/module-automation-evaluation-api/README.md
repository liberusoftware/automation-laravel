# Evaluation API

One-to-one HTTP API adapter for `module-automation-evaluation`. Routes are versioned under `/api/v1/automation/evaluation`, require authenticated team context, and delegate domain mutations to the core package.

