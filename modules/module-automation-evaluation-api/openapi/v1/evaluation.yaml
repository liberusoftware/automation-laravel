openapi: 3.1.0
info:
  title: evaluation API
  version: 1.0.0
paths:
  /api/v1/automation/evaluation:
    get:
      operationId: automation.evaluation.list
      security: [{ sanctum: [] }]
      responses: { '200': { description: Authorized team-scoped resources } }
    post:
      operationId: automation.evaluation.create
      security: [{ sanctum: [] }]
      responses: { '201': { description: Created resource } }
components:
  securitySchemes:
    sanctum: { type: http, scheme: bearer, bearerFormat: Sanctum }

