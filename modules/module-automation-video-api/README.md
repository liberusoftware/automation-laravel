# Video API

One-to-one HTTP API adapter for `module-automation-video`. Routes are versioned under `/api/v1/automation/video`, require authenticated team context, and delegate domain mutations to the core package.

