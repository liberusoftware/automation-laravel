openapi: 3.1.0
info:
  title: video API
  version: 1.0.0
paths:
  /api/v1/automation/video:
    get:
      operationId: automation.video.list
      security: [{ sanctum: [] }]
      responses: { '200': { description: Authorized team-scoped resources } }
    post:
      operationId: automation.video.create
      security: [{ sanctum: [] }]
      responses: { '201': { description: Created resource } }
components:
  securitySchemes:
    sanctum: { type: http, scheme: bearer, bearerFormat: Sanctum }

