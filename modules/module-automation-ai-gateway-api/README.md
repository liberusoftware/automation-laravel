# AiGateway API

One-to-one HTTP API adapter for `module-automation-ai-gateway`. Routes are versioned under `/api/v1/automation/ai-gateway`, require authenticated team context, and delegate domain mutations to the core package.

