openapi: 3.1.0
info:
  title: ai-gateway API
  version: 1.0.0
paths:
  /api/v1/automation/ai-gateway:
    get:
      operationId: automation.ai-gateway.list
      security: [{ sanctum: [] }]
      responses: { '200': { description: Authorized team-scoped resources } }
    post:
      operationId: automation.ai-gateway.create
      security: [{ sanctum: [] }]
      responses: { '201': { description: Created resource } }
components:
  securitySchemes:
    sanctum: { type: http, scheme: bearer, bearerFormat: Sanctum }

