# Connectors API

One-to-one HTTP API adapter for `module-automation-connectors`. Routes are versioned under `/api/v1/automation/connectors`, require authenticated team context, and delegate domain mutations to the core package.

