openapi: 3.1.0
info:
  title: connectors API
  version: 1.0.0
paths:
  /api/v1/automation/connectors:
    get:
      operationId: automation.connectors.list
      security: [{ sanctum: [] }]
      responses: { '200': { description: Authorized team-scoped resources } }
    post:
      operationId: automation.connectors.create
      security: [{ sanctum: [] }]
      responses: { '201': { description: Created resource } }
components:
  securitySchemes:
    sanctum: { type: http, scheme: bearer, bearerFormat: Sanctum }

