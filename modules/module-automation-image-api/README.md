# Image API

One-to-one HTTP API adapter for `module-automation-image`. Routes are versioned under `/api/v1/automation/image`, require authenticated team context, and delegate domain mutations to the core package.

