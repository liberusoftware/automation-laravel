# Liberu Platform Orchestration

The core package owns composition manifests, capability declarations, lifecycle transitions, and release evidence. It stores platform metadata only; product modules remain authoritative for their records and actions.

The matching API, Filament, and Livewire adapters are separate one-to-one packages. Install this package with `liberusoftware/module-liberu-platform-orchestration` and run the published migration.
