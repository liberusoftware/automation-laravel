# Rules API

One-to-one HTTP API adapter for `module-automation-rules`. Routes are versioned under `/api/v1/automation/rules`, require authenticated team context, and delegate domain mutations to the core package.

