# AutomationCore API

One-to-one HTTP API adapter for `module-automation-automation-core`. Routes are versioned under `/api/v1/automation/automation-core`, require authenticated team context, and delegate domain mutations to the core package.

