openapi: 3.1.0
info:
  title: automation-core API
  version: 1.0.0
paths:
  /api/v1/automation/automation-core:
    get:
      operationId: automation.automation-core.list
      security: [{ sanctum: [] }]
      responses: { '200': { description: Authorized team-scoped resources } }
    post:
      operationId: automation.automation-core.create
      security: [{ sanctum: [] }]
      responses: { '201': { description: Created resource } }
components:
  securitySchemes:
    sanctum: { type: http, scheme: bearer, bearerFormat: Sanctum }

