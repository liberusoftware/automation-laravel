apiVersion: apps/v1
kind: Deployment
metadata:
  name: automation-horizon
  namespace: automation-laravel
  labels:
    app: automation-laravel
    component: horizon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: automation-laravel
      component: horizon
  template:
    metadata:
      labels:
        app: automation-laravel
        component: horizon
    spec:
      containers:
      - name: horizon
        image: ghcr.io/liberusoftware/automation-laravel:latest
        command: ["php", "artisan", "horizon"]
        envFrom:
        - configMapRef:
            name: automation-config
        - secretRef:
            name: automation-secrets
        volumeMounts:
        - name: storage
          mountPath: /var/www/html/storage
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command: ["php", "artisan", "horizon:status"]
          initialDelaySeconds: 30
          periodSeconds: 30
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: automation-storage
      securityContext:
        fsGroup: 1000
